'use client'

/**
 * /costing route (SPEC-M1 §9): re-homes the existing CostingView view.
 */
import { CostingView } from '@/components/erp/costing-view'

export default function Page() {
  return <CostingView />
}
